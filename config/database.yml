default: &default
  adapter: mysql2
  encoding: utf8mb4
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: rails_user
  password: rails_password
  host: localhost

development:
  <<: *default
  database: store_development

test:
  <<: *default
  database: store_test